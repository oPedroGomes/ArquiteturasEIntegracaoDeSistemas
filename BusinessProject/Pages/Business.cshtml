@page
@model BusinessProject.Pages.BusinessModel
@{

}

@if (Model.direcoes != null)
{
    <table>
        <thead> <tr> <th>Origin</th> <th>Destination</th> <th>Distance</th> <th>Duration</th> </tr> </thead>
        <tbody>

            @{
                var tete = Model.direcoes.Routes.ToList();
                var instructions = tete[0].Legs.ToList();
                var instructions2 = instructions[0].Steps.ToList();

            }
            @foreach (var item in instructions2)
            {
                <tr>
                    <td>@item.Maneuver</td>
                    <td>@item.Duration.Text</td>
                    <td>@item.Distance.Text</td>
                    <td>@Html.Raw(item.Html_instructions)</td>


                </tr>

            }
        </tbody>
    </table>
}
else
{
    <p>There is no data available for directions</p>
}
@if (Model.parking != null)
{
    <table>
        <thead>
            <tr>
                <th>Nome</th>
                <th>Rating</th>
                <th>Estado</th>
                <!-- Add more columns for each property you want to display -->
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.parking.Places.ToList())
            {
                <tr>
                    <td>@item.DisplayName.Text</td>
                    <td>@item.Rating</td>
                    <td>@item.BusinessStatus</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>There is no data available for parking</p>
}

@if (Model.lazer != null)
{
    @if (Model.lazer != null)
    {
        <table>
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Rating</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var prop in Model.lazer.Places.ToList())
                {
                    <tr>
                        <td>@prop.DisplayName.Text</td>
                        <td>@prop.Rating</td>
                        <td>@prop.BusinessStatus</td>

                    </tr>
                }
            </tbody>
        </table>
    }
}
else
{
    <p>There is no data available for leisure</p>
}

@if (Model.tempo != null)
{
    <table>
        <thead>
            <tr>
                <th>Nome</th>
                <th>Rating</th>
                <th>Estado</th>
            </tr>
        </thead>
        <tbody>



            @foreach (var prop in Model.tempo.Hourly.Temperature_2m.ToList())
            {
                <tr>
                    <td>@prop.ToString()</td>
                    

                </tr>
            }
        </tbody>
    </table>

}
else
{
    <p>There is no data available for Tempo</p>
}



